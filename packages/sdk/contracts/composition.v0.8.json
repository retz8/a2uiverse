{
  "name": "a2uiverse-composition",
  "version": "0.8.0",
  "description": "The A2UIVerse composition extension (SPEC §14): the A2A metadata contract for cross-agent UI composition, internal to the platform (orchestrator ↔ client). A canvas is an A2A context: an utterance that opens one carries no contextId, the orchestrator mints it, and every later message in the canvas carries it. Outbound, the composition stamp (provenance, role) on every relayed event, the paintMeta part naming a paint ahead of its createSurface — the orchestrator's own for the layout surface, carrying the Planner's title for the canvas — and the synthesis payload — the derived data model of formula leaves, its match claims, and the sort declarations of a merged view — on the event that paints a synthesis surface; inbound, the parent canvas on the utterance that opens a child, and the composition operation — the reader's press on the composition: Retry, Include, Try again, a fragment's step back, the canvas closed — on a message of its own. Nothing a2uiverse-specific rides the vendor wire. Normative for every language projection; each projection's contract test asserts against this file.",
  "extensionUri": "https://a2uiverse.dev/ext/composition/v0.8",
  "stampKey": "a2uiverse",
  "surfaceIdSeparator": ":",
  "surfaceIdNamespacing": {
    "form": "<appId><surfaceIdSeparator><surfaceId>",
    "appliedTo": ["createSurface", "updateComponents", "updateDataModel", "deleteSurface"],
    "rule": "The orchestrator namespaces surfaceId on the four A2UI server→client message types and reverses it on inbound client actions. Component ids, binding paths, and catalog ids are never rewritten."
  },
  "synthesisKey": "a2uiverseSynthesis",
  "matchKey": "match",
  "shapes": {
    "compositionStamp": {
      "direction": "orchestrator → client",
      "carriedOn": "every relayed A2A event's metadata, keyed by stampKey",
      "required": ["source"],
      "optional": ["role", "settled", "vendorContextId", "vendorTaskId"],
      "fields": {
        "source": "Provenance and placement: the app id the event came from. A fragment's surface fills the layout's Slot whose source is this id.",
        "role": "Which surface the canvas renders as the composition root: 'shell' | 'fragment'.",
        "vendorContextId": "Debug only, gated by orchestrator config.",
        "vendorTaskId": "Debug only, gated by orchestrator config.",
        "settled": "This source's stream has ended: set on one event the orchestrator emits after relaying a fragment source's last event, with no A2UI parts. The client judges that source's fragments there instead of at the turn's end."
      }
    },
    "canvasParent": {
      "direction": "client → orchestrator",
      "carriedOn": "the opening utterance's message metadata, keyed by stampKey — the key is two-directional: the stamp outbound, the parent inbound; absent on a root canvas",
      "required": ["parent"],
      "optional": [],
      "fields": {
        "parent": "The contextId of the canvas this question was asked from: the trail's edge, and the canvas the Planner reads as this canvas, its ancestry as the recent turns."
      }
    },
    "compositionOperation": {
      "direction": "client → orchestrator",
      "carriedOn": "a data part of its own, {version, operation}, on a new A2A message in the canvas's context — beside the A2UI action, the error report and the utterance; version is the A2UI version the client speaks",
      "required": ["kind", "sources"],
      "optional": ["step"],
      "kinds": ["retry", "include", "tryAgain", "step", "close"],
      "fields": {
        "kind": "The press: 'retry' re-dispatches one failed source's request; 'include' folds the sources that arrived after the merge into it; 'tryAgain' makes again a merge whose call failed; 'step' reports a fragment stepped back or forward in its own history — the paint's data model rides a2uiClientDataModel as on an action; 'close' ends the canvas: everything running in it cancelled, its composition dropped.",
        "sources": "The source ids the press names: exactly one with 'retry' and 'step', at least one with 'include', none with 'tryAgain' and 'close'.",
        "step": "With 'step' only, required there: the index in that agent's stack — one step per surface replacement, counted the same way on both sides, from 0 — the fragment now shows. A non-negative integer."
      }
    },
    "paintMeta": {
      "direction": "orchestrator → client",
      "carriedOn": "a data part of its own, {paintMeta: {...}} with no version field, its part metadata mimeType paintMetaMimeType, emitted ahead of the createSurface it names; the A2UI extractor never takes it",
      "required": ["surfaceId"],
      "optional": ["title", "kind"],
      "kinds": ["question"],
      "fields": {
        "surfaceId": "The surface the paint creates, as the client will see it — namespaced for a fragment, shell:main for the layout surface.",
        "title": "A short title for the paint, at most paintMetaTitleMaxLength characters. On the layout surface it is the Planner's title for the canvas, naming its entry in the trail; a longer one is clipped with an ellipsis by the orchestrator and journaled, never a plan finding; absent, the client falls back to the question. On a fragment it is the agent's own title for the paint, naming the step in the fragment's history.",
        "kind": "The paint's declared kind: 'question' marks a surface the canvas routes to its overlay slot."
      },
      "authors": "The orchestrator emits the part for its own layout surface. A vendor agent's part is the agent kit's, in this same shape, relayed untouched; the contract describes the part, it does not own the kit's emission — an agent that never emits one composes, its paints titled from their cause."
    },
    "synthesizeDataModel": {
      "direction": "orchestrator → client",
      "carriedOn": "metadata.a2uiverseSynthesis of the event that paints a synthesis surface; the tree is painted as ordinary A2UI on the same event",
      "authors": "The orchestrator's Synthesizer writes the merged view; the orchestrator validates it, paints its tree in the shell catalog, and sends the derived data model and the sorts as the synthesis payload.",
      "schemas": {
        "synthesis": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "title": "SynthesisPayload",
          "description": "Client-facing: the synthesis branch minus the tree (painted as A2UI) and the note (journaled). Evaluated client-side into the synthesis surface's data model.",
          "$defs": {
            "ref": {
              "type": "object",
              "additionalProperties": false,
              "required": ["surface", "pointer"],
              "description": "A cross-partition qualified ref: a pointer that resolves in another surface's data model.",
              "properties": {
                "surface": {
                  "type": "string",
                  "description": "The namespaced surfaceId (<appId>:<surfaceId>) the pointer resolves in."
                },
                "pointer": {
                  "type": "string",
                  "pattern": "^(|/.*)$",
                  "description": "RFC 6901 JSON Pointer into that surface's data model, where a segment may carry a predicate: `items[sku=\"x\"]` selects the element of `items` whose field `sku` equals the JSON literal \"x\", and `items[a=\"x\",b=2]` conjoins several field tests until exactly one element matches. Elements are selected by key, never by position: a positional segment into an array does not resolve. A ref is valid while its keys resolve; none matching and several matching are both absent. The value is a JSON literal compared by JSON equality."
                }
              }
            },
            "formula": {
              "type": "object",
              "additionalProperties": false,
              "required": ["op", "args"],
              "description": "A leaf of the derived model: one operator over N refs, N ≥ 0. A plain vendor value is a one-argument pass-through. A formula with no refs is a value no source contributes to — absent by construction. Operators are names the shell catalog declares as functions; the contract does not enumerate them. Recognized by shape: an object with exactly `op` and `args` is a formula.",
              "properties": {
                "op": {
                  "type": "string",
                  "description": "A function name declared by the shell catalog."
                },
                "args": {
                  "type": "array",
                  "minItems": 0,
                  "items": {
                    "$ref": "#/$defs/ref"
                  }
                }
              }
            },
            "relation": {
              "type": "object",
              "additionalProperties": false,
              "required": ["op", "args"],
              "description": "One named relation of a match claim: a formula like any other leaf, its operator a relation the shell catalog declares, over exactly two refs in two different apps — the appId of each ref's namespaced surface. The contract does not enumerate relations; the consumer checks the operator against the shell catalog.",
              "properties": {
                "op": {
                  "type": "string",
                  "description": "A relation the shell catalog declares as a function."
                },
                "args": {
                  "type": "array",
                  "minItems": 2,
                  "maxItems": 2,
                  "items": {
                    "$ref": "#/$defs/ref"
                  }
                }
              }
            },
            "match": {
              "type": "object",
              "minProperties": 1,
              "additionalProperties": {
                "$ref": "#/$defs/relation"
              },
              "description": "A match claim, under the reserved key `match` of an object of the derived model, the root included: the Synthesizer's word that the entries the object joins from different apps are one thing, with its evidence. Each key is the Synthesizer's own words for what matched, each value a relation. At least one relation; flat. No object is required to carry one. Disclosed on the object's values by the shell's derived-value component; the tree binds no path under `match`."
            },
            "node": {
              "description": "A node of the free-form derived model: a formula leaf, an object whose values are nodes — its key `match`, when present, is its match claim — or an array of nodes. A scalar anywhere is a violation — every leaf is a formula, never a literal value.",
              "oneOf": [
                {
                  "$ref": "#/$defs/formula"
                },
                {
                  "type": "object",
                  "properties": {
                    "match": {
                      "$ref": "#/$defs/match"
                    }
                  },
                  "additionalProperties": {
                    "$ref": "#/$defs/node"
                  }
                },
                {
                  "type": "array",
                  "items": {
                    "$ref": "#/$defs/node"
                  }
                }
              ]
            },
            "dataModel": {
              "type": "object",
              "minProperties": 1,
              "propertyNames": {
                "not": {
                  "const": "sorts"
                }
              },
              "properties": {
                "match": {
                  "$ref": "#/$defs/match"
                }
              },
              "additionalProperties": {
                "$ref": "#/$defs/node"
              },
              "description": "The derived data model the tree binds to: a JSON shape of the Synthesizer's choosing whose every leaf is a formula. Evaluated client-side into plain values with contributor state at the same paths. The key `match` on any object, the root included, is that object's match claim. The root key `sorts` is reserved: the runtime writes each sort declaration, with the user's current choice, at `/sorts/N` of the synthesis surface's data model, and the tree binds SortControl there."
            },
            "sort": {
              "type": "object",
              "additionalProperties": false,
              "required": ["path", "options", "key", "direction"],
              "description": "A sort declaration over one array of the derived model, or — through `*` — over the list inside every element of the arrays enclosing it, all of them ordered alike. The model names the array, the sortable keys and their labels, and the initial choice; the runtime sorts and keeps the user's choice of key or direction, one choice for every list the declaration reaches.",
              "properties": {
                "path": {
                  "type": "string",
                  "pattern": "^/.*$",
                  "description": "JSON Pointer to the sorted array in the derived model. Its steps are object keys and `*`: a `*` passes through every element of an array, at any depth, so `/rows/*/runs` names the list `runs` inside every element of `rows`. Every element the path passes through carries the list, `[]` when it has none. The last step names the array; an array is never stepped into by position, and no step selects an element by key."
                },
                "options": {
                  "type": "array",
                  "minItems": 1,
                  "description": "The keys the user may sort by, each with its user-facing label.",
                  "items": {
                    "type": "object",
                    "additionalProperties": false,
                    "required": ["key", "label"],
                    "properties": {
                      "key": {
                        "type": "string",
                        "pattern": "^/.*$",
                        "description": "JSON Pointer inside each element of the sorted array — of every list the path reaches — to a formula leaf."
                      },
                      "label": {
                        "type": "string",
                        "minLength": 1
                      }
                    }
                  }
                },
                "key": {
                  "type": "string",
                  "description": "The initial key; one of the options."
                },
                "direction": {
                  "type": "string",
                  "enum": ["asc", "desc"]
                }
              }
            }
          },
          "type": "object",
          "additionalProperties": false,
          "required": ["dataModel", "sorts"],
          "properties": {
            "dataModel": {
              "$ref": "#/$defs/dataModel"
            },
            "sorts": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/sort"
              }
            }
          }
        }
      }
    }
  },
  "canvasIdentity": {
    "is": "the A2A contextId",
    "rule": "Each canvas is its own A2A context. The utterance that opens a canvas is sent with no contextId; the orchestrator mints one and the client reads it from the first event. Every later message in the canvas — action, press, error report — carries it. The orchestrator holds one composition per context for the session; a vendor's context is per canvas."
  },
  "paintMetaMimeType": "application/json+a2ui-shell",
  "paintMetaTitleMaxLength": 48
}
