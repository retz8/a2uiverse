/** @a2uiverse/shell-catalog — the shell's catalog. Schema and implementation land with Phase 1–2. */
import {SDK_NAME} from '@a2uiverse/sdk';

export const SHELL_CATALOG_NAME = '@a2uiverse/shell-catalog';
export const SHELL_CATALOG_CONTRACT = SDK_NAME;
