import {SHELL_CATALOG_NAME} from '@a2uiverse/shell-catalog';

export function App() {
  // Placeholder: the canvas shell is ported in Phase 1.
  return <main>A2UIVerse — scaffold only · {SHELL_CATALOG_NAME}</main>;
}
