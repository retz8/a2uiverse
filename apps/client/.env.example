# The orchestrator's A2A base URL — the only server the client talks to.
VITE_ORCHESTRATOR_URL=http://localhost:10001
# Through the dev tunnel (see _dev/docs/tunnel-environment.md), in an uncommitted .env.local:
# VITE_ORCHESTRATOR_URL=https://vnw20xbg-10001.asse.devtunnels.ms
