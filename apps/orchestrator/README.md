# @a2uiverse/orchestrator

The hub (SPEC §10–11): an A2A agent server the canvas talks to **exclusively**. It routes a question to the agents that can answer it, plans where their answers will sit, dispatches them in parallel, and relays what comes back as one composed turn.

Registry · Embedder · Router · Planner · Synthesizer · composition · AgentsPool · IntentJournal behind one A2A executor. One model call per utterance turn — the Planner — and a second, the Synthesizer, when the plan reserves a merged view and at least two sources answer.

## How a turn works

```
utterance → Router (embed + rank, the platform's card among the candidates)
          → Planner (one model call, text out; the platform readers as steps inside it; one retry)
          → shell paint: the model-authored layout, every slot pending, gap slots as the capability tile
          → fan-out: one dispatch per vendor slot, in parallel — none for a platform answer or a gap
          → each fragment relayed as it arrives
          → slot repaints for whatever failed or fell silent, the failure's cause on the slot
          → Synthesizer when the trigger releases it (one model call, text out, one retry)
            → the merged view painted into its slot
          → one turn-final once every dispatch has arrived, failed or reached the hard cap
```

**The trigger.** A merge is possible once two sources have arrived — the home source among them under a join. When every source has settled the synthesis runs; while some are still out, the **soft deadline** (10 s with no source settling) releases it over what arrived, and a straggler then mounts in its own slot with no model call. The home source is never waited out: without it nothing is joined, and when it fails the merged view collapses at once with no call. Every collapse leaves one line where the view's label would have sat — the Synthesizer's reason for a decline, the shell's words for the rest (`collapse` on the slot). The turn's first synthesis is its only automatic one; the merge keeps the set of sources it was built over, and a re-synthesis after an action runs over that set.

**The hard cap.** 300 s after a dispatch, its slot fails with `timeout`. The dispatch runs on: an answer arriving past the cap is held on the slot, undrawn, until Retry. The turn's journal line closes when its last dispatch ends. A new utterance in the same conversation ends the turn before it — its dispatches aborted and each vendor sent A2A's `tasks/cancel`, its model calls aborted, anything held dropped — and its line is marked `superseded`.

Two properties fall out of that order and are worth knowing before reading the code:

**First paint precedes every dispatch.** The layout and its pending slots reach the canvas before any agent has been asked anything, so time-to-first-paint is the Planner's latency, not the slowest agent's.

**One agent failing never fails the turn.** Each vendor runs its own A2A task; the hub demotes their finals and emits a single one of its own once every dispatch has settled. A slot whose dispatch failed is repainted `failed` with its cause — `vendor` (with the vendor's own words, taken off its final), `unreachable`, `timeout`, or `invalid` when the client reports the paint could not be drawn — and its data leaves the merge; one whose agent answered without a surface is `collapsed`.

The relay is otherwise transparent. It rewrites exactly one thing in the vendor's A2UI — namespacing `surfaceId` to `<appId>:<surfaceId>`, reversed on inbound actions — and never touches component ids, binding paths or catalog ids. Around that A2UI it stamps composition metadata and demotes terminal states, and on the way _out_ it filters each dispatch to the vendor's own partition. SPEC §14 carries the full register.

## Modules

| Module               | Where              | Stack                                                                                                                                              | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Registry             | `src/registry/`    | plain TS; `@a2a-js/sdk` `DefaultAgentCardResolver` at boot                                                                                         | Installed-app records (hardcoded until M7) plus the agent-authored mirror: AgentCards fetched at startup (nullable — unreachable ⇒ unroutable this session) and one corpus vector per app. The platform's own card (`src/agentCard.ts` — hand-authored skills: what A2UIVerse is, what the canvas can do, installed apps, finding and installing apps) is indexed under the reserved `shell` id beside them, so the Router ranks the platform like any app; it is not an installed app and nothing dispatches to it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Embedder             | `src/embedder/`    | `@huggingface/transformers` (transformers.js), quantized `Xenova/all-MiniLM-L6-v2`, in-process                                                     | The one embedding model — loaded once, no API key, cached under `STATE_DIR/models`; injected into Registry, Router, and the journal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Router               | `src/router/`      | plain TS (cosine over Embedder vectors)                                                                                                            | Retrieval only: embeds the utterance, ranks routable apps, returns a capped shortlist — no threshold; selection is the Planner's                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Planner              | `src/planner/`     | Vercel AI SDK (`ai` v7) + `@ai-sdk/google`, Gemini Flash, text call with the readers as tools; `@a2uiverse/sdk` A2UI validator and catalog pruning | The turn's one model call, the shell's designer and voice (SPEC §5.6): from the utterance and the shortlist's cards — the platform's own among them — it writes the layout surface as text in one `<layout-surface>` block: the dispatch list (each agent's prose request, the merged view's brief as source `shell`, capability gaps), the `shell:main` tree in the shell catalog pruned to `LAYOUT_SURFACE_KEEP_SET` (a `Slot` per dispatch entry, framing, platform answers), and a data model of literals. Platform state reaches it through three readers called on demand — installed apps, this canvas, recent turns — never vendor data. Prompt: role, rules doc (`planner.md`), the catalog's `platform-ui-guidance.md`, the pruned catalog, the output schema, the worked examples. One validator: output schema → tree through the sdk's A2UI validator → slot accounting → painter-owned props → literal-only data; one retry in the same conversation, a second failure a broken turn. The journal records the document, every attempt and every reader call |
| Composition          | `src/composition/` | pure TS; paints the `@a2uiverse/shell-catalog`                                                                                                     | Shell-surface painter (`shell:main`: the Planner's tree with each vendor `Slot` wrapped in an `Attribution` holding it as child and carrying its weight, the synthesis slot and gap slots bare, `state`/`label` written by the painter, the literal data model sent first), fragment relay (stamp · surfaceId namespacing · final demotion), outbound partition filter, turn classification, partitions, the IntegrityChecker (per-ref validity over the sdk kit; the change account), the synthesis painter (`shell:synthesis`, the model's tree verbatim, the payload under `a2uiverseSynthesis`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Synthesizer          | `src/synthesizer/` | Vercel AI SDK text call; `@a2uiverse/sdk` A2UI validator, catalog pruning and payload validator                                                    | The turn's second model call, authored here: its role, rules doc (`synthesis.md`), worked examples, output schema, validator and turn building. The prompt shows the shell catalog pruned to the synthesis surface's keep-set beside `synthesis-guidance.md`; the model writes the synthesize data model as text in one `<synthesize-data-model>` block, read by the shared extractor in `src/authoring/`; one validator: output schema → derived model and sorts through the sdk → tree through the sdk's A2UI validator against the pruned catalog → derived-value rule → operators → refs; one retry carrying the findings and the failed document, then `malformed`. A re-synthesis is handed the previous document and what broke. The journal records every attempt                                                                                                                                                                                                                                                                                                 |
| AgentsPool           | `src/agentsPool/`  | `@a2a-js/sdk` client (JSON-RPC, streaming)                                                                                                         | Pure transport to vendor agents: per-turn dispatch handles, id-space relay, the hard cap, a failure's cause and the vendor's words, cancel sent on to the vendor, the dev-only fault map; knows nothing of plans or slots                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| IntentJournal        | `src/journal/`     | append-only JSONL under `STATE_DIR`; an in-memory ring of five entries per conversation                                                            | One line per turn — descriptor, the plan (the layout surface, its attempts, the reader calls), synthesis, dispatch records, surface touches — with the descriptor embedded at write time by the Embedder; the ring backs the Planner's recent-turns reader and starts empty at boot                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| OrchestratorExecutor | `src/executor.ts`  | `@a2a-js/sdk` server (`AgentExecutor`)                                                                                                             | The turn itself, in the order above; holds per-conversation composition state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## Running it

```bash
pnpm dev:orch                                     # from the repo root
pnpm --filter @a2uiverse/orchestrator start       # node dist/index.js
pnpm --filter @a2uiverse/orchestrator build | typecheck | test | lint
```

It needs the vendor agents already listening — `pnpm dev:agents`, or `pnpm dev:all` which sequences both. **Cards are fetched once, at boot.** An agent that comes up later is unroutable for the whole session, so the orchestrator names the ones it could not reach and says how to fix it. If the canvas routes nothing, read that line first.

## Configuration

| Variable                          | Default                                         | Meaning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PORT`                            | `10001`                                         | Listen port                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `BASE_URL`                        | `http://localhost:<PORT>`                       | URL advertised in the agent card — set to the tunnel URL in tunnel sessions                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `STATE_DIR`                       | `./.state` (cwd-relative)                       | Local state: `intent-journal.jsonl`, the cached embedding model                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `A2UIVERSE_AGENT_URLS`            | —                                               | JSON object `{"<appId>": "<url>"}` overriding registry agent URLs                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `A2UIVERSE_AGENTS_DIR`            | —                                               | Roster from the manifests one level below this directory, replacing the hardcoded one. Set by the launcher on the platform it starts, from its `--agents-dir` or the same variable — how the mock tier is opted in                                                                                                                                                                                                                                                                                                                     |
| `A2UIVERSE_DEBUG_IDS`             | off                                             | `1`/`true`: include vendor ids under `metadata.a2uiverse` on relayed events                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `GOOGLE_API_KEY`                  | —                                               | Google AI Studio key for the Planner; unset ⇒ palette turns fail (actions still route)                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `A2UIVERSE_PLANNER_MODEL`         | `gemini-3.7-flash`                              | Planner model id                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `A2UIVERSE_PLANNER_EFFORT`        | `low`                                           | `low` (no thinking budget) or `default` — this is time-to-first-paint; whether tree authoring needs thinking is measured in 6.6                                                                                                                                                                                                                                                                                                                                                                                                        |
| `A2UIVERSE_SHORTLIST_CAP`         | `5`                                             | Router shortlist size cap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `A2UIVERSE_SYNTHESIZER_MODEL`     | the Planner's when set, else `gemini-3.7-flash` | Synthesizer model id (the turn's second call)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `A2UIVERSE_SYNTHESIZER_EFFORT`    | `low`                                           | `low` or `default`; dead air is measured before effort is spent (journal `synthesis.deadAirMs`)                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `A2UIVERSE_SOFT_DEADLINE_SECONDS` | `10`                                            | Once a merge is possible, how long with no source settling releases the synthesis without the stragglers. A changed value is logged at boot                                                                                                                                                                                                                                                                                                                                                                                            |
| `A2UIVERSE_HARD_CAP_SECONDS`      | `300`                                           | From each dispatch to its slot failing with `timeout`; the dispatch runs on and its answer is held. A changed value is logged at boot                                                                                                                                                                                                                                                                                                                                                                                                  |
| `A2UIVERSE_FAULTS`                | —                                               | Dev only. JSON `{"<appId>": {"fault": "delay"\|"hang"\|"break"\|"refuse"\|"fail"\|"invalid", "seconds"?, "message"?, "every"?}}`: the AgentsPool applies the fault to that source's plan dispatch (every dispatch with `"every": true`) — `delay` holds the stream `seconds`, `hang` never answers, `break` ends after the first paint with no final, `refuse` fails the connection, `fail` ends failed with `message`, `invalid` renames a component of the paint; `seconds` delays the others. Vendors are untouched. Logged at boot |

## Registry

Hardcoded until M7 (`src/registry/entries.ts`): `github` → `:11001`, `gmail` → `:11002`, `calendar` → `:11003`. Each record is orchestrator-authored install state; the AgentCard beside it is the agent's own account of itself, refetched at every boot and authoritative for anything the agent declares.

With `A2UIVERSE_AGENTS_DIR` set, the roster is instead read from that directory (`src/registry/manifests.ts`): every immediate child carrying a `manifest.json` becomes a record, the launcher's own convention. A child without a manifest is not an app; a manifest that is malformed or missing a field fails boot naming the file; a directory yielding no records fails boot. `A2UIVERSE_AGENT_URLS` applies per id on top either way. No platform source names a mock: `pnpm dev:all --agents-dir ../a2uiverse-apps/mocks` is the whole opt-in — the launcher sets the variable for the orchestrator it starts.

## Dependencies

`@a2uiverse/sdk`, `@a2uiverse/shell-catalog`, `@a2a-js/sdk` (0.3 line), `ai` + `@ai-sdk/google`, `@huggingface/transformers`, Express. Vendor agents are reached over A2A only.

## Port

`10001`. The advertised base URL is set per environment — see `_dev/docs/tunnel-environment.md`.
