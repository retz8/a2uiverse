node_modules
.DS_Store
dist
*.tsbuildinfo
.env
.env.local
.env.*.local
.vite/
coverage/

# Python (uv-managed)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Playwright
test-results/
playwright-report/

# Claude Code worktrees (native EnterWorktree) + SDD scratch ledger
.claude/worktrees/
.superpowers/

# Personal, machine-local Claude instructions (auto-loaded alongside CLAUDE.md)
CLAUDE.local.md

# Turborepo
.turbo/

# Per-machine Claude Code settings
.claude/settings.local.json

# Orchestrator local state (intent journal; registry from M7)
.state/

# Google OAuth client secrets — developer-local, never committed (Workspace MCP setup)
client_secret_*.json
