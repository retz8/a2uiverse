# A2UIVerse — Dev TODO

## Phase 0 — Scaffold [done]
Monorepo, workspace packages, root commands to run the local processes, per-package READMEs, sibling `a2uiverse-apps` skeleton. No behavior.
Spec: `_dev/docs/spec/phase-0-scaffold.md`.

- [x] **0.1** Doc edits — SPEC §9/§13/§15, `a2ui-sdk-design` skill (both repos), `a2uiverse-apps` README + CLAUDE.md
- [x] **0.2** Monorepo scaffold — pnpm + Turborepo, toolchain, `apps/{client,orchestrator,marketplace}` + `packages/{sdk,shell-catalog}`, READMEs, placeholder `dev`, gates green (after 0.1; parallel with 0.3)
- [x] **0.3** `[apps]` `a2uiverse-apps` skeleton — workspace, one-folder-per-app layout, CLAUDE.md setup + gates, gates green with no vendor (after 0.1; parallel with 0.2)
- [x] **0.4** Nightly routine setup, `a2uiverse` — labels, fresh-clone gate check, `schedule` registration, CLAUDE.md (after 0.2; parallel with 0.5)
- [x] **0.5** `[apps]` Nightly routine setup, `a2uiverse-apps` (after 0.3; parallel with 0.4)

## Phase 1 — Spine [done]
Client ↔ orchestrator ↔ one vendor agent, end to end: canvas shell ported from `a2ui-github`, orchestrator as an A2A agent server, hardcoded registry, intent journal (M0). The client talks only to the orchestrator.
Spec: `_dev/docs/spec/phase-1-spine.md`.

- [x] **1.1** Doc edits — SPEC wording/roster/registry card duty/backlog, TODO roadmap amendment (Phase 2 three agents, agent-kit phase, renumber), tunnel docs port ranges (both repos), `a2uiverse-apps` CLAUDE.md + README wording
- [x] **1.2** Orchestrator — A2A server on 0.3.14, port 10001, minimal static card, hardcoded Registry with bundle record, AgentsPool transparent relay + source stamp, IntentJournal (after 1.1; parallel with 1.3)
- [x] **1.3** Client port — canvas only, Radix Themes shell, Primer scoped to fragment, `orchestratorApi` static catalog map, `link:` primer-a2ui-adapter, replayBeat, tests (after 1.1; parallel with 1.2)
- [x] **1.4** Spine integration + acceptance — client → orchestrator wiring, `pnpm dev` (vendor agent started by hand from `a2ui-github`), READMEs, beat re-recording + visual Playwright spec, transparency/action/journal/card checks, Claude-in-Chrome live verification via tunnel (after 1.2 and 1.3)
- [x] **1.5** `[apps]` GitHub app copy — `a2uiverse-apps/github/`, port 11001, catalog id fixed, `github-catalog` published with its provider + CSS setup owned by the bundle, client on published package, gates green in both repos (after 1.4)

## Phase 2 — Layout-only composition [done]
3 agents · 3 catalogs on one surface, no synthesis (M1): GitHub (copied in 1.5), Gmail, Google Calendar. Basic Router, Planner, AgentsPool fan-out, flat-surfaces composition (placement on A2A metadata), shell catalog composition primitives, plan/fill/collapse, provenance + attribution, partition isolation. Deferred from 1.3: unknown-component handling at the composition layer; shell re-skin on Radix tokens with the shell catalog. Deferred from 1.4: `pnpm dev:agents` launcher + `dev:all`. The vendor-agent template/CLI moved to Phase 3.
Spec: `_dev/docs/spec/phase-2-layout-composition.md`.

- [x] **2.1** Doc edits — SPEC §4.1/§4.2/§4.3/§13/§14/§16 amendments per spec decision 18
- [x] **2.2** `sdk` composition extension — `contracts/` JSON + `js/` + `python/` projections, contract tests, workspace/turbo wiring incl. Python toolchain, both published (after 2.1)
- [x] **2.3** `shell-catalog` — schema + basic-catalog implementation, Radix-bound token theme, `Slot` + `Attribution` (after 2.1; parallel with 2.2)
- [x] **2.4** Orchestrator composition core — embedder + Registry card surface + Router, Planner (AI SDK seam, plan schema, per-agent requests), fan-out dispatch, three relay rewrites (stamp · surfaceId namespace · partition filter), slot-lifecycle painting, journal embedding (after 2.2; parallel with 2.3, 2.5–2.8)
- [x] **2.5** Client composition — multi-catalog processor, placement map, fragment boundary, Slot mounting, validation + `VALIDATION_FAILED` reporting, ChoicePicker pnpm patch, collision detector (after 2.2 and 2.3; parallel with 2.4, 2.6–2.8)
- [x] **2.6** `[apps]` Gmail app — three-mode agent on 11002 against live Gmail MCP, `gmail-catalog` (basic + product tokens) published, its four beats recorded (after 2.2; parallel with 2.4, 2.5, 2.7, 2.8)
- [x] **2.7** `[apps]` Calendar app — three-mode agent on 11003 against live Calendar MCP, `calendar-catalog` published, its beats recorded (after 2.2; parallel with 2.4, 2.5, 2.6, 2.8)
- [x] **2.8** `[apps]` `github-catalog` boundary retrofit — scoped provider, anchored portal root, republish; AgentCard retrofitted as a retrieval document (task-2.6 decision 14 — the three cards must be comparable embedding targets or the Router skews); empty the accepted-violation list in the client's collision detector; 2.5's live tunnel verification rides this session (after 2.1; parallel with 2.2–2.7)
- [x] **2.9** Integration + acceptance — `dev:agents` launcher + `dev:all` + `dev:client`/`dev:orch`/`dev:marketplace` aliases, the composed fan-out beat recorded over live MCP, attributed prose channel (stamp `onAgentText`, buffer per source, plural notices), visual Playwright specs, acceptance items 1–11, Claude-in-Chrome live verification via tunnel (after 2.4–2.8)
- [x] **2.10** README sweep — both repos, against the phase's end state: three agents, composition on orchestrator/client, run commands per 2.9's launchers (after 2.9)
- [x] **2.11** `[apps]` Vendor fragment visual pass — Gmail + Calendar product theme sheets (scoped stylesheet layer, SPEC §14), detector ships leading-compound classes only; trees, brand docs and fixtures untouched — the defects were catalog-implementation-level (after 2.6 and 2.7; before 2.9's beat recording)
- [x] **2.12** Live visual review — all three agents in `llm` mode over real MCP, a real conversation set driven through the canvas: does the composed screen read as one screen, and is this the thing we meant to build (after 2.10)

## Phase 3 — Agent building kit [done]
Shared vendor-agent logic extracted across the three agents into a published A2UI+A2A agent SDK/CLI in `a2uiverse-apps` (M1k). Uniform run-mode flag, single launcher for installed agents, vendor dependency rule amended, one-provider-and-CSS-setup-per-catalog-bundle rule.
Spec: `_dev/docs/spec/phase-3-agent-kit.md`.

- [x] **3.1** Doc edits — SPEC §13 dependency-rule amendment + stale `@a2uiverse/sdk` catalog-half claim reconciled, §14 `paintMeta` delta row, one-provider-and-CSS-setup-per-catalog-bundle review rule (#1)
- [x] **3.2** `[apps]` SDK package + stratum-1 extraction — Python kit (executor, servers, recorder, catalog load/validation, prompt assembly, `paint_meta`, beat pipeline), uniform `--mode deterministic|stub|live` entrypoint, per-vendor config, three agents refactored on path deps, shared tests into the kit, beats replay unchanged (after 3.1)
- [x] **3.3** `[apps]` Stratum-2 skeletons — toolset-wrapper base class, MCP-wiring + tool-shaping skeletons/hooks in the kit; vendor policy bodies rehomed onto them (after 3.2; parallel with 3.4)
- [x] **3.4** `[apps]` Scaffold CLI — TS workspace package, scaffold flow with the basic-themed/custom catalog option, generated agent on a version-pinned git dep, manifest emission; carries 3.3's deferred opt-in questions: Google ADC credential block (does the vendor need it), a2uiverse-ecosystem readiness (for now just paintMeta wiring) (after 3.2; parallel with 3.3)
- [x] **3.5** Launcher — platform `dev:agents` rewritten table-free: manifest-glob discovery over the agents dir, `--mode` passthrough to the kit entrypoint, agent list command, existing flag parity (after 3.2)
- [x] **3.6** Integration + acceptance — `dev:all` end to end, fourth-app scaffold proof (basic variant boots + paints, custom variant gates, then deleted) — note the orchestrator's own hardcoded registry: a scaffolded app is launchable by existing but not routable without a registry entry or `A2UIVERSE_AGENT_URLS` override, README/run-command updates both repos, live tunnel sanity pass (after 3.3–3.5)
- [x] **3.7** `[apps]` GitHub agent write tier — off the read-only endpoint onto the full toolset surface (capability = whatever MCP + token allow, no confinement), proposal/toggling convention as domain-doc guidance, read-only identity sweep (prose, card, README, tests), live tunnel verification; beats/fixtures untouched (after 3.3)

## Phase 4 — Synthesis, identical shapes [done]
Two sibling mock vendors merged (M2). Synthesizer, derived bindings, BindingEvaluator, IntegrityChecker, generation stamps, disclosure. Shapes identical by construction, so the join is free and every remaining hard thing is machinery. Mocks are quarantined from the default roster and kept as the clean-room regression bed.
Spec: `_dev/docs/spec/phase-4-synthesis.md`.

- [x] **4.1** Doc edits — SPEC §5.4 disclosure amendment, §6.2–§6.3 absent/invalid naming, §14 register corrections + cross-partition ref row, `a2uiverse-apps` docs for the mock tier
- [x] **4.2** `sdk` — synthesis wiring contract (rows, refs, formulas, generation stamps, the metadata channel), contract tests, published (after 4.1; parallel with 4.3, 4.6)
- [x] **4.3** `shell-catalog` — operator function definitions, the derived-value component, the sort control (after 4.1; parallel with 4.2, 4.6)
- [x] **4.4** Orchestrator synthesis core — partition materialization, Synthesizer, IntegrityChecker + generation stamps, plan-schema reservation + Planner prompt, synthesis surface painting, decline → collapse (after 4.2; parallel with 4.5, 4.6, 4.7)
- [x] **4.5** Client synthesis — wiring intake, BindingEvaluator, derived-model evaluation + data-model write, sort, partial-value integration (after 4.2 and 4.3; parallel with 4.4, 4.6, 4.7)
- [x] **4.6** `[apps]` Two mock storefronts — scaffolded into the quarantined tier, shared product dataset, three run modes with `live` over an in-repo dataset, both instrument behaviours (drill-down → absent; in-place reorder → invalid), beats recorded (after 4.1; parallel with 4.2–4.5)
- [x] **4.7** Mock profile plumbing — launcher discovery opt-in and registry opt-in (after 4.6; parallel with 4.4, 4.5)
- [x] **4.8** Integration + acceptance — end to end under the profile, both instruments proven, decline, sort, partial-value visibility, dead-air measured, live tunnel verification (after 4.4–4.7)

## Phase 5 — Heterogeneous shapes [done]
Temporal merge over Calendar · Mail · GitHub (M3). Unrelated data models, shared-axis merge, key-based refs, decline, quiescence across unsynchronized arrivals. The Synthesizer authors the merged view — the synthesize data model: a shell-catalog tree, a free-form derived data model with formula leaves, sorts, a note — replacing the Phase 4 wiring and derived table.
Spec: `_dev/docs/spec/phase-5-heterogeneous-shapes.md`.

- [x] **5.1** Doc edits — SPEC §5 turn line and §5.2, §10 Synthesizer row, §12 M3 line, §14 rows for synthesis wiring, path predicates, derived-value rule
- [x] **5.2** `sdk` — composition contract v0.3: the synthesize data model (tree, formula-leaf model, predicate refs, sorts, note), the JS projection, contract tests, published (after 5.1; parallel with 5.3, 5.6)
- [x] **5.3** `shell-catalog` — `SortControl` over a sort declaration at the reserved `/sorts/N`, built on Radix Themes; Provider brings a scoped Radix `Theme` + stylesheet as the bundle's own design system; `catalog.json` authoring semantics for `DerivedValue`/`SortControl`; sdk reserves the root key `sorts`; `DerivedValue` untouched (after 5.1; parallel with 5.2, 5.6)
- [x] **5.4** Orchestrator — Synthesizer rewrite: text output, validate against contract and catalog, one retry; the prompt as the composition-authoring document in a2uiverse words; checklist with the derived-value rule and predicate resolution; predicate refs in the IntegrityChecker over the sdk kit; painter paints the model-authored tree; re-synthesis handed its previous output and the change account; the whole conversation journaled; sdk prompt builder + composition doc + examples, shell-catalog guidance doc and React-free schema export. Planner prompt: the synthesis brief and the vendor merge asks (after 5.2 and 5.3; parallel with 5.5)
- [x] **5.5** Client — BindingEvaluator over the free-form model with predicate refs, sorts over declared arrays, v0.3 intake validated by the sdk, stale marker over the sdk kit, the synthesis region rendered as shell content (no boundary, no attribution, quiet pending marker keyed on the stamp's `source: shell`) with the SPEC §4.3/§4.5 amendment, the Phase 4 `SortObject` alias removed, the legacy wiring (`wiring.ts`, `composition.v0.2.json`) removed from the sdk, synthesis fixture re-recorded (after 5.2 and 5.3; parallel with 5.4)
- [x] **5.6** `[apps]` S1 beats — the "today" turn recorded per vendor in live mode, agents unmodified (after 5.1; parallel with 5.2–5.5)
- [x] **5.7** Integration + acceptance — temporal merge end to end over the real roster live, comparison over the mocks, drill-down → absent and reorder → no re-synthesis under predicates, re-synthesis with previous output, decline, quiescence with real latency spread, dead air measured, live tunnel verification; a client-level test that a dropped key degrades gracefully end to end — 5.10 removed the `INDEXED_*`/`REPOINTED_*` fixtures with the mechanism they exercised, leaving that case covered at the evaluator only; the shared axis sorts as strings — gmail paints `2026-09-05 01:24 UTC`, github `2026-09-05T00:03:52Z`, so a same-day github entry orders after every gmail one whatever its time (found in 5.11) (after 5.4–5.6, 5.9, 5.10, 5.11)
- [x] **5.8** Design records — `synthesis.md` rewritten, Synthesizer sections of the orchestrator and client records; `packages/sdk` README rewritten — what the sdk exposes, stated plainly and readable without prior knowledge of the synthesize data model (after 5.7)
- [x] **5.9** `shell-catalog` Radix Themes mapping — each basic component implemented on its Radix Themes counterpart, the way `primer-a2ui-adapter` maps onto Primer; the token-themed re-export goes; SPEC §4.2 amendment + §15 row; fixture matrix; parity test (after 5.3; parallel with 5.4, 5.5; before 5.7)
- [x] **5.10** `sdk`/`shell-catalog`/orchestrator/client — synthesis refs collapse to one key-based form: compound keys, index refs removed from the contract and rejected by name, re-synthesis triggered by absence, the generation baseline and the `stale` cell state removed, contract v0.4, SPEC §6.2/§6.3/§14 and phase decision 6 amended (after 5.5; before 5.7)
- [x] **5.11** `sdk` — the Synthesizer's worked examples rewritten against task 5.6's recorded shapes: heterogeneous kinds, opaque ids, a source whose time is not ISO (after 5.6 and 5.10; before 5.7)

## Phase 6 — The shell as an agent [done]
The orchestrator's model answers an utterance itself, in the shell catalog, when no vendor serves it — installed agents, what the canvas can do, the platform's own state (M3s). The platform's own AgentCard in the Registry, ranked by the Router. The Planner is the shell's designer and voice, one call: it authors `shell:main` as a shell-catalog tree — layout, framing content, platform answers — `direction` and `archetype` gone, `Attribution` wrapped by code; platform state reaches it as a closed set of readers called on demand, never vendor partitions. The shell knows what, deterministic pages are how: the Store page and the App Library stay trusted routes the model may only paint an affordance into. A capability gap is a `Slot` with a gap source, filled by the fixed tile. Two shell actions, declared in the shell catalog, journaled. First paint measured before streaming is spent — a streaming sub-task is added after 6.6 if the number calls for it. Before Phases 8, 11 and 14, which paint shell content into slots.
Spec: `_dev/docs/spec/phase-6-shell-as-agent.md`.

- [x] **6.1** Doc edits — SPEC §4.4 (archetypes), §5 (the turn: the Planner's readers and tree), §8 (the gap as a slot the Planner places), §10 (the Planner's row, "never sees data" resolved as never vendor data; the Registry row, the platform's card indexed under `shell`), §12 M3s line, §7 (the shell actions' route), §4.3 (the shell's own content beyond the synthesis surface); every other section a Phase 6 decision makes untrue
- [x] **6.2** `shell-catalog` — the shell's action set declared in the schema (open the Store with an optional query, open the App Library); the shell catalog's two guidance docs, `guidance.md` renamed `synthesis-guidance.md` (content unchanged, the Synthesizer's one consumer line updated) and a new `platform-ui-guidance.md` — how the shell draws UI about A2UIVerse itself — shipped and exported, not yet read by the Planner; the catalog built by a factory taking the host's shell-action handler; a `gap` state on `Slot` drawing the fixed capability tile — a minimal line and a button raising `openStore` with the capability as query; if the action form departs from A2UI — anything other than catalog-declared client-side functions invoked by `functionCall`, like the basic catalog's `openUrl` — add its row to SPEC §14 (left out in 6.1) (after 6.1; parallel with 6.3)
- [x] **6.3** Restructure, no visible change — sdk as generic tools and the wire contract: A2UI v0.9.1 validator following upstream's `A2uiValidator` (pinned spec schemas generated from `upstream/main`, conformance cases in its tests), catalog pruning by keep-set, contract v0.5 (the stamp drops `slot` and places by `source`; `synthesizeDataModel` leaves the contract); shell catalog: `Slot`'s `name` → `source`, `gap`, `weight`, two keep-sets, `synthesis-guidance.md` drops its component bans; the Synthesizer's role, `synthesis.md`, examples, output schema, consolidated validator and turn building into orchestrator/synthesizer on its pruned catalog; one shared tagged-block extractor; painter and client on `source`, recordings follow; SPEC §4.1/§14 and READMEs. Spec: `_dev/docs/spec/task-6.3-restructure.md` (after 6.2; before 6.4 and 6.5)
- [x] **6.4** Orchestrator — the platform's card: hand-authored skills on the orchestrator's own AgentCard, indexed in the Registry under `shell`; Planner rewrite on 6.3's base, from its carried decisions: the output schema `{dispatch, tree, dataModel}` (entries `{source, request}` or `{gap}`), its role, rules doc, prompt and turn building in orchestrator/planner on the Planner's pruned catalog and `platform-ui-guidance.md`, text output through the shared extractor, one retry; all Planner validation in orchestrator/planner (output schema, tree through the sdk's A2UI validator, slot accounting, merged view only with two or more sources, literal-only data, shortlist); the three readers as the Planner's closed tool set behind one seam; painter wraps every `Slot` in `Attribution` and fills gap slots with the fixed tile; `direction`/`archetype` removed; `Frame` removed from the shell catalog with the painter change that frees it — the Planner sizes slots with `weight`, carried onto each `Slot`'s `Attribution` wrapper; journal records the tree, the tool calls, the gaps (after 6.3; parallel with 6.5)
- [x] **6.5** Client — `shell:main`'s literal data model arriving as plain A2UI; the roster pairing `Attribution` to `Slot` through `child`, `weight` rendered; shell-source actions handled locally — placeholder overlay naming the page and query, reported to the orchestrator as an action turn for the journal, with the orchestrator's intake of a shell-source action (journaled, nothing painted); fixtures re-recorded plus a platform-answer beat and a gap beat. Spec: `_dev/docs/spec/task-6.5-client-shell-surface.md` (after 6.3 and 6.4)
- [x] **6.6** Integration + acceptance — spec decision 10 over the real roster live through the tunnel: the four platform questions, the mixed utterance, the gap, the S1/single-agent/mock-comparison regression with the "side by side" control prompt, the vocabulary checks; first paint measured and recorded, the streaming decision taken against it: deferred to the backlog (Planner 3.3–3.7 s on the temporal merge against 15 s to the first fragment). Fixed during the run: no Planner heading over a screen of slots, a reader called is a reader written from; the shell-action report is verified in code, not through the tunnel. Spec: `_dev/docs/spec/task-6.6-integration-acceptance.md` (after 6.4 and 6.5)
- [x] **6.7** Design records — the Planner, Registry and painter sections of `orchestrator.md`, the client record's shell-surface and action handling, the shell-catalog record's action set (after 6.6)

## Phase 7 — Entity resolution [done]
Entity join (M4): the differentiator proven, over real agents — a work item across Linear · GitHub · CircleCI on the `a2uiverse` repository, two new vendor apps written to their vendor, never to the merge. Resolution is top-down: the Planner states the join hypothesis in prose — the entity and each source's cue — and the Synthesizer confirms it per merged object as a checked match claim: relation formulas over new shell-catalog operators, validated at accept, evaluated live on the client. The join is disclosed on the values, through the derived-value component, escalating by state. The home source defines the rows. Navigation from a merged cell to the originating subtree (SPEC §7, deferred from Phase 5): every derived-value cell naming a source navigates, landing degrades to the nearest bound ancestor then the fragment, nothing leaves the client, not journaled. Appearance fires re-synthesis as the second half of the IntegrityChecker's walk. Contract v0.6.
Spec: `_dev/docs/spec/phase-7-entity-resolution.md`.

- [x] **7.1** Doc edits — SPEC §3 (the entity join row), §5.2 (the match claim; the brief names entity and cues), §5.4 (the join disclosed), §6.3 (appearance handled), §7 (landing, degradation, not journaled), §9.4 (roster), §10 (the Synthesizer's row; the settled shape replaces "shape only; not settled"), §12 M4 line, §14 (relation operators, the join-disclosure rule, the match claim); apps-repo README + CLAUDE.md roster; both repos' tunnel port tables
- [x] **7.2** `[apps]` CircleCI app — scaffolded on the kit, backed by the official hosted MCP server, three modes, basic catalog themed, manifest, port; written to its vendor; the CircleCI project on this repository as its setup; the platform's registry entry and client catalog wiring. Spec: `_dev/docs/spec/task-7.2-circleci-app.md`. Handoff: `_dev/docs/handoff/task-7.2.md` (after 7.1; parallel with 7.3–7.5)
- [x] **7.3** `[apps]` Linear app — scaffolded on the kit, backed by the official MCP server, three modes, basic catalog themed, manifest, port; written to its vendor; the Linear workspace with its GitHub integration on this repository as its setup; whether the server returns an issue's linked pull request or branch name checked empirically and recorded, issue text as the fallback; the platform's registry entry and client catalog wiring. Spec: `_dev/docs/spec/task-7.3-linear-app.md`. Handoff: `_dev/docs/handoff/task-7.3.md` (after 7.1; parallel with 7.2, 7.4, 7.5)
- [x] **7.4** `sdk` — composition contract v0.6: the match claim as a word of the synthesize data model's language — the reserved key `match` on any object, named relation formulas, flat, each over two refs in two different apps, written where the Synthesizer judges a join, no rule on which objects carry one; the sdk's validator checks its shape; JS projection, contract tests, published; the stamp's `generations` removed end to end (contract, sdk, the orchestrator's snapshot, bump and stamping, the client's leftovers); the design records and the sdk README for what it changes. Spec: `_dev/docs/spec/task-7.4-match-claim-contract.md` (after 7.1; parallel with 7.2, 7.3, 7.5)
- [x] **7.5** `shell-catalog` — relations `equal`, `contains` and `judged`, declared and executed, exported apart from the operators: text as token sequences, numbers and instants by value, lists of plain values as lists; `CellObject` gains its join (mark and evidence) and its navigation target; the mark rule as a pure function the evaluator calls; `DerivedValue` draws the join on the values — guessed, broken, the detail on hover or focus — and is the navigation button; `createCatalog` takes the host's navigation handler and app-name lookup; the Synthesizer's keep-set gains the relations; the orchestrator's validator and the client's intake check that a match claim is written in relations and that no relation stands outside one; the synthesis guidance doc gains the join; the contract's `match` description follows. Spec: `_dev/docs/spec/task-7.5-relations-join-on-values.md` (after 7.1; parallel with 7.2–7.4)
- [x] **7.6** Orchestrator — Planner: the join hypothesis in the merged view's request, entity, home source and per-source cue, the vendor requests asking for the cue fields, an orders example over fixture cards; Synthesizer: the rules doc's join — the hypothesis as the starting point, lists inside a row with their count, decline without home rows — the camera comparison out of the prompt, validator rules (every fact of a match claim holds against the partitions, run on the shell catalog's relation functions, its finding never offering `judged`; the tree binds no path under `match`; the nested checks); IntegrityChecker: the watched arrays' key sets at every accept, appeared entries and relations no longer holding in the change account, broken fires nothing; journal. Spec: `_dev/docs/spec/task-7.6-orchestrator-entity-join.md` (after 7.4, 7.5 and 7.12; parallel with 7.7)
- [x] **7.7** Client — evaluator computes relation formulas, each cell's join (its mark and evidence, per 7.5) and its navigation target; a nested sort applied to every row's list (7.12); the reverse index from data path to rendered component per partition; navigation: scroll, focus, highlight, degrading to the nearest bound ancestor, the fragment boundary, then the slot, no request; the host's navigation handler and app-name lookup wired into the shell catalog through one relay bound to the mounted canvas; the sdk's located form of pointer resolution. Spec: `_dev/docs/spec/task-7.7-client-entity-join.md` (after 7.4, 7.5 and 7.12; parallel with 7.6)
- [x] **7.8** `[apps]` Entity-join beats — the pinned utterance recorded per vendor in live mode over this repository's work, agents unmodified; the prompts are the requests the 7.6 Planner writes, read from the journal; the Planner's fan-out stance first — it answers the question the user means (SPEC §7, its rules doc, a minimal pair over the orders cards), measured live. Spec: `_dev/docs/spec/task-7.8-entity-join-beats.md` (after 7.2, 7.3 and 7.6; parallel with 7.7)
- [x] **7.9** Integration + acceptance — spec decision 13's nine items: code first, then the mocks deterministic, the real roster deterministic, the real roster live through the tunnel; the broken state, and a list inside a row under the nested sort, on a synthetic client beat; an absent cell is not a button (phase decision 9, task 7.5 decision 12 and SPEC §7 follow); vanish gains the home source, nothing false left standing; appear asserts that the row tells the truth after a live rerun, pull request #8 given a Linear issue for it; item 1 on three landings in a row deterministic and one live; the mocks' comparison, reorder and decline compose, the match claim observed; the regression turns deterministic; beat 9, the entity-join turn; dead air recorded; the four failing visual baselines diagnosed and the e2e suite green before any pass; the palette's dropped input watched; the landing ring's duration judged by the user. Spec: `_dev/docs/spec/task-7.9-integration-acceptance.md` (after 7.6–7.8 and 7.13)
- [x] **7.10** Design records — `synthesis.md` rewritten; the Planner, Synthesizer and IntegrityChecker sections of the orchestrator record; the client record's evaluator, reverse index and navigation; the shell-catalog record's relations and the join on the values; an absent cell is not a button (task 7.9 decision 2); the orchestrator record's partitions keeping one surface per source, the walk's repainted surfaces and the refusal of a message id received twice, the client record's app names read off the mounted shell paint and its first-event timeout with one retry, the shell-catalog record's cell that names an app (found in 7.9); `packages/sdk` README (after 7.9)
- [x] **7.11** `[apps]` Product components on a themed basic catalog — SPEC §9.2 amendment and §14 rows (the appended-component convention; a bindable tone on the basic `Text` as the upstream candidate); CircleCI's `StatusBadge`, its status pill in colors sampled from CircleCI's web app, the agent onto it, beats re-recorded, the platform on the new catalog; Linear (7.3) uses the rule (after 7.2's build)
- [x] **7.12** `sdk` — the nested sort path: a sort declaration's path may pass through the enclosing arrays with `*`, one declaration ordering a list inside every row; the sdk's validator, contract tests; contract v0.6 amended in place; SPEC §5.2's sort declarations and the §14 synthesize data model row (after 7.4; before 7.6 and 7.7)
- [x] **7.13** Orchestrator — the entity join's synthesis lands on the pinned utterance: the Synthesizer's validator hands back every finding on each attempt, a faulty sort declaration withholding none; measured on the deterministic bed, three landings in a row; closed by the Synthesizer on `gemini-3.7-flash` at low effort, now its default; A2U-7 moved to In Progress in the Linear workspace and Linear's beats re-recorded; no worked example in the prompt; the temporal merge lands. Spec: `_dev/docs/spec/task-7.13-entity-join-synthesis.md` (after 7.6 and 7.8; before 7.9)
- [x] **7.14** The question as the header — the user's words verbatim, the one fixed header at the top of every turn from Enter until the next question, no screen title; a paragraph at body size cut at 4 lines, "Show all" opening it in full over the page; the progress line under it, a tick per source then the join in computed words; attribution captions re-spaced; the status strip no longer carries the question; the Planner stops restating the question. Matches boards F1–F3 and F7–F9 of the design canvas (https://claude.ai/artifact/W324EkZXFze2CxddzNve1o). (after 7.10; parallel with 7.16)
- [x] **7.15** The reserved merge slot — from plan time the merged view's slot holds its planned column headers over four skeleton rows, the landed view taking its own height, a declined merge collapsing as SPEC §5.1 states; the Planner writes the merged view's `columns` and the join's nouns on its dispatch entry, painted onto the slot as `Slot` props, the Synthesizer starting from the columns and saying its departures in the note; the progress line's join in the entity's nouns ("Joining Linear issues to GitHub PRs and CircleCI runs"); `Table` drawn to F3; SPEC §4.3, §4.5, §5.2, §5.6; beat 9 re-recorded over the deterministic roster. Matches boards F2, F3 and F8 of the design canvas (after 7.14)
- [x] **7.16** The table reads as an answer — the merged view's duplicated columns trimmed (a pull request as "#8"), the model's title only as the merged view's small label, a semantic red for a failure apart from the certainty gray and the broken amber; the Synthesizer's rules, the shell catalog's tone, the SPEC §14 row. Matches board F3 of the design canvas (after 7.10; parallel with 7.14)

## Phase 8 — Late arrival + failure [done]
Late arrival and failure (M6). Per-source deadlines in the AgentsPool: a soft deadline as patience after the pack releases the turn's one automatic synthesis, the stragglers' dispatches running on until a hard cap fails them, an answer past the cap held until Retry; under a join the home source is exempt and the reserved slot waits for it; the turn's final waits for every dispatch to arrive, fail or reach the cap, and a new utterance ends it. A source the merge landed without keeps its planned column, reserved, its cells drawn from the slot's state. A late arrival mounts free; Include, on the merged view's label row, folds it in on the reader's press. The failure tile drawn as board F6 of the 7.14 design canvas (https://claude.ai/artifact/W324EkZXFze2CxddzNve1o): the failure said in its slot at body size in the client's words, a retry, the vendor's words beneath, no top bubble; the cause and the vendor's words ride on the `Slot`; Retry re-dispatches one slot and includes on arrival. One automatic synthesis per turn; every further call has a press behind it. The declined merge's collapse revisited (from 7.15): the reserved merged view gives its space back and the fragments move up once, one quiet line in the Synthesizer's words left; every other collapse leaves a line in the client's words. A dev-only fault map in the AgentsPool.
Spec: `_dev/docs/spec/phase-8-late-arrival-failure.md`.

- [x] **8.1** Doc edits — SPEC §4.5 (the declined merge's collapse geometry), §5.3 (the arrival visible and attributed, the absorb on request), §14 rows (the failure cause and retry on the `Slot`, the column's source mark, the Include operation)
- [x] **8.2** `shell-catalog` — the `Slot`'s failure props and its failed branch drawn to board F6 with the client's line and a host-wired Retry; the Table's per-column source mark and the reserved column's cells from slot state; the collapsed slot as one quiet line giving the skeleton's space back. Spec: `_dev/docs/spec/task-8.2-shell-catalog-failure-reserved-column.md` (after 8.1; parallel with 8.3)
- [x] **8.3** Orchestrator, deadlines — AgentsPool: the soft deadline as 10 s of quiet once a merge is possible, the 300 s hard cap failing the slot with `timeout` and holding a later answer, both from the environment, the home source exempt under a join, the final waiting for every dispatch to arrive, fail or reach the cap; a new utterance ending the in-flight turn on the orchestrator, A2A's cancel sent to the vendors it aborts; the failure's cause and the vendor's words set on the repaint, a half-drawn or invalid source out of the merge; the painter's `noun` on each vendor slot and the decline's reason as `declined` on the shell slot; every collapse of the merge leaving one line — the collapse cause painted, the shell catalog composing the line; a failed home source collapsing without a call; the merge's own source set; the Planner's column-to-source mark from plan time, required; the Synthesizer handed the marks and the missing sources, keeping their columns; the dev-only fault map; journal. Spec: `_dev/docs/spec/task-8.3-orchestrator-deadlines.md` (after 8.1; parallel with 8.2 and 8.9)
- [x] **8.4** Orchestrator, Include, Retry and Try again — the three presses as operations on the composition: one Include for every late source waiting, a press covering what it named, a failed re-synthesis keeping the landed view with a line and the press that tries again; Retry back to loading, drawing a held answer at once, racing a capped dispatch still running — the slot following the re-dispatch — or re-dispatching, a retried source rejoining the pack before the first merge and folded in on arrival after it; an arrival never restarting a merge in the making, everything owed run as one call; a collapsed merge brought back, made like a first merge, moving nothing until it lands; Include after a decline, Try again on a merge that couldn't be made; presses beside the running turn, ended by a new utterance; the Synthesizer handed the composition's question; the facts on the shell `Slot`; the operation as its own data part in the composition contract v0.7, on its own stream; journal. Spec: `_dev/docs/spec/task-8.4-orchestrator-include-retry.md` (after 8.3 and 8.10)
- [x] **8.5** Client — the three presses sent as the composition contract's operation, each on a stream of its own beside the turn, ended by a new utterance; a failed source's fragment taken off the canvas; the press lines composed by the shell catalog from the facts on the shell `Slot` — a row above the merged view's label, the collapsed merge's working, waiting and couldn't-be-made lines, Include under a decline's line — behind one press handler; the pressed state drawn at the click, a press that never arrived or lost its stream said in place, buttons disabled while a newer question replaces the composition and in a parked one; focus and announcements; the progress line's ticks and merge step; the reserved column's not included; the composition's states kept across the actions inside it; the decline read from the paint, the orchestrator's prose copy removed, the reader's view held still; tests, the design-check fixture, a live sanity pass. Spec: `_dev/docs/spec/task-8.5-client-presses-and-lines.md` (after 8.2 and 8.4)
- [x] **8.6** Beats — the presses and the answer to a failure report as streams beside the turn in a beat, fired at their recorded time through the handler the buttons call and answered from the beat; synthetic beats for a fast failure with Retry, a late arrival with Include, the home source straggling, an answer held past the cap drawn by Retry, a half-drawn fragment failing, an invalid paint, each collapse's line, the Retry race, a failed fold-in, Include after a decline, Try again, a collapsed merge brought back by Retry, each with a press also up to it; recorded beats 10–18 over the deterministic roster through the fault map, the recorder starting an orchestrator with each case's faults and deadlines; the fault map's `break` and `invalid` made to reach their cases; tests and the visual spec. Spec: `_dev/docs/spec/task-8.6-beats.md` (after 8.5)
- [x] **8.7** Integration + acceptance — the twelve cases once on the deterministic roster through the tunnel, watched live one at a time with the reader's UX and design points fixed on the spot (decisions 16–31: the strip, the tile as one statement, the marker, the pending line, the merge step honest, the press rows as the view's action, the collapse lines carrying Retry, the decline in ink, the condensed bar, the Table's first column, the progress line taking no room, the union join, the heartbeat); the four live cases; the regression pass; the invariant tabulated from the journal; found and fixed: the column marks as surface ids, an undrawable paint reaching the merge, a Retry's re-dispatch outliving its composition, the tunnel cutting a silent turn at 100 s; baselines taken at the end. Spec: `_dev/docs/spec/task-8.7-integration-acceptance.md` (after 8.6)
- [x] **8.8** Design records — synthesis.md's trigger and decline, and what 8.6 and 8.7 find: the join hypothesis's two kinds (8.7 decision 30), the settled stamp and the merge thrown away on an undrawable report (25), the heartbeat (31), the press rows' register and the collapse lines' presses (21, 23, 24), the failure tile as one statement (17), the merge step's states (20), the Table's column sizing (29), and the rest of 8.7's look — the strip on Back's edge, the marker without "Painted by", the pending slot's spinner, the condensed bar keeping the progress line, the strip leaving a fragment's failure to its tile, the decline in ink, the progress line taking no room (16, 18, 19, 22, 26–28); the delta register's rows for the settled stamp and the collapse's failed list; SPEC's failure tile (§8, §14), §4.5's collapse line carrying its press, §5.1–§5.3's join hypothesis by kind; the phase spec's decisions 4, 7, 8 and 12 marked amended by 8.7 (after 8.7)
- [x] **8.9** `[apps]` The kit honours A2A's cancel — both executors accept `tasks/cancel`, the running model and MCP work stopped, tests. Spec: `_dev/docs/spec/task-8.9-kit-honours-cancel.md` (after 8.1; parallel with 8.3)
- [x] **8.10** Orchestrator, quiescence — a source with a request in flight, from the plan or from the user, holds the synthesis; a synthesis whose partitions change while it runs is invalidated, not reconciled, and re-fires on quiescence (SPEC §5.3). Spec: `_dev/docs/spec/task-8.10-orchestrator-quiescence.md` (after 8.3; before 8.4)

## Phase 9 — Durable composition [WIP]
Durable composition (M5). The trail as one entry per question, drawn flat by time as board F5 of the 7.14 design canvas (https://claude.ai/artifact/W324EkZXFze2CxddzNve1o): titled by the Planner, branches annotated with the canvas they were asked from, opened on demand, a preview on hover, a close on each entry. A past canvas is a previous tab, not a frozen past: actable — actions, presses, sort — its answers landing in it, running on after the user leaves it, stamped with its question's time, closed by the user, never evicted. Asking from a view — a new question, "Ask this again now", add/drop source, "compare these" — makes a child canvas planned from the viewed one; there is no reload in place. Each agent's paints inside a canvas are local state: a back/forward stack per agent, a back arrow beside the attribution marker, a step back restoring the paint and the merged view's remembered wiring with no call. Every message names its canvas; the orchestrator holds a composition per canvas for the session. The 4.8 finding (an action from a parked view landing nowhere), the 7.16 finding (a drill-down with no way back) and the 8.4 finding (a parked composition as a previous tab) are closed here.
Spec: `_dev/docs/spec/phase-9-durable-composition.md`.

- [x] **9.1** Doc edits — SPEC §6.4 rewritten (a past canvas as a tab: not frozen, stamped with its question's time, asked again from the view, never reloaded in place), §5.6 (the Planner's title), §7 (add/drop source and "compare these" as words at the palette making a child canvas), §10 (a composition per canvas), §4.3 (the fragment's back and forward beside the attribution marker), §14 rows (the canvas named on every client message; the shell's own `paintMeta` for `shell:main`)
- [x] **9.2** `sdk` — the composition contract: the canvas named on every client message in place of `a2uiForkContext`, the parent canvas on an utterance asked from a view, the Planner's title in the plan and as `paintMeta` for `shell:main`; the projection and its contract test (after 9.1)
- [x] **9.3** Orchestrator, canvases — a composition per canvas for the session, every message routed to the canvas it names; a new utterance no longer ending the previous turn — its dispatches, deadlines, merge and presses running on; a close cancelling a loading canvas's turn; the child canvas's parent recorded, the this-canvas and recent-turns readers over the viewed canvas and its ancestry; the Planner's title, validated, emitted as `paintMeta` ahead of the shell create; journal (after 9.2)
- [x] **9.4** Orchestrator, the fragment's history — each surface replacement a step in that agent's stack on the composition, a step back reported by the client putting that paint's data model back on the partition; the accepted wiring remembered per combination of steps and restored without a call when the combination was seen, the walk otherwise; journal (after 9.3)
- [x] **9.5** `shell-catalog` — the attribution row's back and forward arrows, host-wired like Retry, present only with somewhere to go, named by the previous paint's title, "Back" without one (after 9.1; parallel with 9.2–9.4)
- [x] **9.6** Client, the trail and the past canvas as a tab — entries per canvas titled by the Planner's `paintMeta` with the question as fallback, flat by time with "from" on a branch, Live, Viewing and still-loading marks, a close per entry, the hover preview, the panel opened from a Trail button beside Back as board F5; the parked banner with the question's time, "Ask this again now" as the question asked from this view, "Return to live"; every message naming the viewed canvas; a past canvas actable — actions, presses, sort — its answers landing in it, tabs running in the background with their own progress line; the ring cap gone; Phase 1's fork hold retired; tests, the design-check fixture, a live sanity pass (after 9.3)
- [x] **9.7** Client, the fragment's way back — the per-agent stacks, the arrows' presses, a step back restoring the paint and re-evaluating the remembered wiring, the partition reported, forward dropped on a new paint; tests, the design-check fixture (after 9.4, 9.5 and 9.6)
- [ ] **9.8** Beats — synthetic and recorded: a tab finishing in the background, an action and a press in a past canvas, "Ask this again now" and a question asked from a view as a child, add/drop and "compare these", a step back with the wiring restored, an unseen combination falling to the walk, closing a loading canvas; tests and the visual spec (after 9.7)
- [ ] **9.9** Integration + acceptance — the cases live through the tunnel, the reader's UX and design points fixed on the spot against board F5, the regression pass, baselines (after 9.8)
- [ ] **9.10** Design records — client.md's trail, tab and fragment history, orchestrator.md's composition per canvas and the running tabs, synthesis.md's remembered wiring, shell-catalog.md's arrows; the delta-register rows; the phase spec's decisions amended by 9.9 (after 9.9)

## Phase 10 — App bundle + registry
Bundle format, local install, registry no longer hardcoded, the GitHub app installed as a bundle (M7).

## Phase 11 — Authority surfaces
Auth-required → authority tile, consent dialog, AuthVault, credential components barred from all catalogs (M8). Expand the orchestrator's slot naming policy for multi-account: slot names derived from appId alone stop being collision-free once one app can fan out under two accounts.

## Phase 12 — Marketplace + publish
Local index, package hosting, publish step, hello-fragment smoke test (M9).

## Phase 13 — Shell trusted pages
Store page, App Library, accounts (M10). Opening a trusted page must not unmount the canvas: the client holds its whole session in memory, created when the canvas mounts, and Phase 14's resume after install needs the canvas that raised the capability gap. The Store and the App Library are an overlay over the canvas (6.5); 6.5's placeholder overlay is the shell to fill. URL addressability, and whether one route with two tabs or two routes, are decided here.

## Phase 14 — Store loop
Capability gap → marketplace index → install → resume (M11).

## Phase 15 — Ecosystem run
Publish a new app → discover → install → compose with an existing one → act inside it. One sitting, no code changes; the deliverable is the recording (M12).

## Backlog
- The Planner reads its plan's `direction` as the slots' axis, but the runtime lays groups along it and the slots of one group across it: "side by side" over one group of two came out stacked (5.7, the control prompt). Phase 6's framing revisit
- The Planner's `archetype` (`card|panel|row|full`) is planned, validated and journaled but affects nothing: `Frame` sizes a row's slots equally regardless. Decide what each value should mean — whether `full` breaks out of its row, whether weights are proportional — or drop it from the plan schema
- Revisit Gmail/Calendar product styling depth once compositions and functionality land at later milestones
- Capability-gap terminal state when the marketplace has nothing either — what the capability tile says, whether the reserved slot collapses or stands (Phase 14's grill)
- Stream the synthesis fragment into its reserved slot (§16 dead-air mitigation). Measured in 4.8 over the mock roster (last source settled → synthesis outcome, journal `synthesis.deadAirMs`, Planner and Synthesizer on `gemini-2.5-flash` at low effort): deterministic mocks — comparison utterance 3.7 s · 4.2 s · 9.2 s across three runs, re-synthesis after an in-place reorder 3.8 s, decline 1.2–1.5 s; live mocks — comparison 8.0 s, re-synthesis after reorder 3.7 s, decline 1.4 s. The interval is the Synthesizer's own call; the mocks' mode barely moves it. Measured again in 5.7 under the text loop (Synthesizer authoring the synthesize data model as text, one attempt every time): deterministic mocks — comparison 6.1 s · 8.5 s, re-synthesis after a drill-down 6.0 s and after the return 8.3 s, decline 1.6 s; deterministic real roster, "What needs my attention today?" — 15.2 s before the run's validator rules, 8.7 s after, re-synthesis after a Gmail drill-down 8.0 s; live real roster — 11.8 s on the canvas, 12.6 s on the recorder's turn, 15.4 s authoring the table form, the live decline probe 1.8 s. Measured in 7.13 on the deterministic real roster, Synthesizer on `gemini-3.7-flash` at low effort, one attempt every time: "what's the status of what I'm working on?" 12.8 s · 14.0 s · 12.5 s, "What needs my attention today?" 19.1 s Measured in 7.9, Synthesizer on `gemini-3.7-flash` at low effort, one attempt every time: deterministic mocks — comparison 11.3 s · 14.6 s, decline 2.3 s; deterministic real roster — "what's the status of what I'm working on?" over Linear, GitHub and CircleCI 10.4 s · 7.0 s · 8.6 s, re-synthesis after opening a CircleCI run 10.2 s · 9.8 s and after opening a Linear issue 12.8 s, "What needs my attention today?" over four sources 18.9 s; live real roster — the same utterance 11.2 s · 10.1 s on the canvas and 11.0 s on the recorder's turn, the three dispatches settling at 27–30 s (Linear), 33–36 s (CircleCI) and 51–65 s (GitHub) after a 6–7 s plan, re-synthesis after opening a CircleCI run 11.1 s, after its rerun proposal 8.2 s, after opening a pull request 13.8 s, after opening a Linear issue 10.7 s.
- A re-synthesis may re-author the merged view, not only re-point its refs: in the live pass the reorder's re-synthesis swapped the per-shop price columns for per-shop availability columns. The Synthesizer is called with the Planner's request alone; whether it should be handed the previous wiring's fields so a bump preserves the view's shape is undecided
- S5 multi-account exercised
- Calendar's in-place RSVP toggle works in an LLM mode for the first time: the kit's executor used to reject every turn that carried no `createSurface` (fixed in 4.6). Verified in stub mode — the turn it now accepts is one `validate_surface` still rejects — but only when the prompt asks for an in-place change; unprompted the model paints a fresh RSVP surface. Record a live beat for it, and decide whether the prose should prefer the in-place turn
- Migrate to A2A 1.0 when `a2ui-agent-sdk` does — client, orchestrator, vendor kit in one move
- A vendor's time format is not stable across live runs (found in 5.7): Gmail painted `2026-09-05 01:24 UTC` on one run and `Sep 6, 2026 · 00:20 UTC` on the next, and the client's sort comparator knows only named shapes, so that run's timeline sorted as strings. Widen the shapes, or a bounded parse of any value carrying a four-digit year and a clock
- A canvas action on the live composed screen failed with "Failed to fetch" after about seventy seconds, nothing reaching the vendor or the journal, the orchestrator answering its card throughout (5.7; 4.8 saw the unclosed-turn shape of it). Not reproduced on demand. Seen three times in 6.6 on the shell-action report alone, through the tunnel: of four presses, the App Library button's report arrived in 1 s; the Store button's first press hung as a fetch for 100 s and never arrived; its second press arrived 40 s late; the capability tile's press failed in the console with "Failed to fetch" about 110 s after. Every utterance through the same tunnel arrived at once, and the recorder's turns on `localhost` matched the orchestrator's timings. The side channel's request is a streaming `message/send` the orchestrator answers with a final status in milliseconds; what the tunnel does with it is the open question Reproduced in 7.9, three of about thirty POSTs through the tunnel, an utterance among them: each hung 100.1 s; a socket-level trace on the orchestrator showed no connection opened for the hung request, every arriving request on a fresh connection from the tunnel's relay, other requests to the same host going through meanwhile — lost before the orchestrator, A2U-7. Since 7.9 the client takes a request with no first event in 10 s for lost and sends it once more under the same message id, the orchestrator refusing an id it has already taken in; the loss itself is the tunnel's
- Stream the Planner's output — the dispatch list ahead of the tree so vendors start before the layout completes, the layout arriving as the model writes it (phase 6 decision 9). Deferred in 6.6 on the first-paint measurement, real roster live, Planner on `gemini-2.5-flash` at low effort, journal `plan.planMs`: temporal merge 3.3 s (recorder) · 3.7 s (browser), the layout on the client 9 ms behind, the first vendor fragment at 18–19 s, the screen settled at 50–55 s; side by side 2.4–2.7 s; platform answers 4.2–6.7 s, which is the whole answer's blank canvas; gap 1.9 s; single agent 2.4 s; mock comparison 3.2 s. The Planner moved to `gemini-3.7-flash` in 7.8 (journal `plan.planMs`, real roster live): the work-status merge 5.3–5.7 s, the temporal merge 4.9–7.2 s, single agent 2.6–5.2 s, a platform answer 4.0–7.1 s
- The Synthesizer's effort: `A2UIVERSE_SYNTHESIZER_EFFORT` has sat at `low` since 4.7, on `gemini-3.7-flash` since 7.13. In 7.13, `default` effort on `gemini-2.5-flash` took the entity join's dead air to 59.5 s and left A2U-6 without its pull request, a fact linking them holding; `gemini-3.7-flash` at `low` landed it. Taken up with the streaming discussion
- Slot expansion — a UX improvement for the case where the user wants to focus on a specific slot: zoom in, a new tab, or something different. Out of 7.7, where navigation lands in the slot as the Planner sized it. Worth revisiting soon
- The Planner's and Synthesizer's model calls carry no time limit and no abort signal: a hung call holds the turn open until the next utterance ends it. No hang observed (slowest synthesis 18.9 s, slowest plan 7.2 s); taken up only if one is
- Re-attaching a stream that broke after it answered, through A2A's `tasks/resubscribe` — the orchestrator's `@a2a-js/sdk` 0.3.14 implements it from the moment of re-attach, nothing replayed — as one design for the plan turn's stream and the press streams (8.5). Until then a press whose stream broke says so in place
- Revisit the shell catalog's prop surface the Primer way — Radix Themes' own props in place of the basic catalog's — once the milestones are complete
